//! Engine operations behind the Organize and Compress methods (kept out of `methods/` so the
//! RPC files stay thin): picture codecs, content geometry, compression.

pub mod compress;
pub mod geometry;
pub mod image;
