{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The only capability: the main window. Native dialogs, reading/writing the files the user picked or dropped (plugin-fs scope is filled by the dialog plugin and the drop handler; no static paths), events, title-bar dragging, and the app's own commands. No shell, no HTTP, no process.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "allow-host-info",
    "allow-app-ready",
    "allow-set-locale",
    "allow-set-menu",
    "allow-print-pdf",
    "allow-suggest-save-path",
    "allow-print-open",
    "allow-print-page",
    "allow-print-close"
  ]
}
